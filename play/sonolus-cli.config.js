/** @type {import('@sonolus/sonolus.js').SonolusCLIConfig} */
export default {
    type: 'play',

    // devServer(sonolus) {
    //     sonolus.level.items[0].bgm = sonolus.add('./.dev/bgm.mp3')
    // },
}
